spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      topic-parsed: parsed_event
      topic-raw: rawlog
      group-id: distributor-service-v1
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.json.value.default.type: com.tritronik.distributor.dto.ParsedEvent
        spring.json.trusted.packages: com.tritronik.distributor.dto
        spring.json.use.type.headers: false
    producer:
      final-destination: analytics_storage_topic
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false